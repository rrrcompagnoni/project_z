version: "3.9"
services:
  geolocation:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/opt/geolocation
    depends_on:
      - db
  db:
    image: postgres:13.1-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    restart: always
